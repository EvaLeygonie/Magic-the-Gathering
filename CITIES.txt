//=== TODO ===//

- addEventListener for each button to each function => displayCities + a text saying what you did

//=== Extra queries/variables ===//

const cityForm = document.querySelector("#form_cities")
const cityId = document.querySelector("#city_id").value
  if (cityId) cityParams.push(`id=${cityId}`)
HTML:
      <input type="text" id="city_id" placeholder="City ID">

Fetch-kod:

  fetch('https://avancera.app/cities/')
  .then((response) => response.json())
  .then((result) => {
    console.log(result)
  })

ALLT FÖRUTOM FETCH:

/* const postButton = document.querySelector("#post_city")
const putButton = document.querySelector("#put_city")
const deleteButton = document.querySelector("#delete_city") */

COMBINED FORM:
    <form id="get_city">

      <input type="text" id="city_name" placeholder="City name">
      <input type="text" id="city_pop" placeholder="Population">

      <input type="submit" id="fetch_city" value="See City">
      <input type="submit" id="post_city" value="Add City">
      <input type="submit" id="put_city" value="Update City">
      <input type="submit" id="delete_city" value="Delete City">

    </form>

OLD ForEach:
   const cityDiv = document.createElement('div')
    cityDiv.className = 'city_div'
    cityDisplay.appendChild(cityDiv)

    const cityDisplayName = document.createElement('p')
    cityDisplayName.className = 'city_display_name'
    cityDisplayName.innerHTML = 'City: ' +  city.name
    cityDiv.appendChild(cityDisplayName)

    const cityDisplayPop = document.createElement('p')
    cityDisplayPop.className = 'city_display_pop'
    cityDisplayPop.innerHTML = 'Population: ' + city.population
    cityDiv.appendChild(cityDisplayPop)

    const deleteButton = document.createElement('button')
    deleteButton.className = 'delete_button'
    deleteButton.innerHTML = 'Delete city'
    deleteButton.onclick = deleteCity(city.id)
    cityDiv.appendChild(deleteButton)
